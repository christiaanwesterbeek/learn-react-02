{
  "name": "learn-react-02",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "json-server": "0.10.0",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "db": "json-server db.json -p 8080 --static ./build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
